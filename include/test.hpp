#pragma once

#include <string>

// Function reads the graph from the "matrix.txt" file located in the provided directory, tests the Bron-Kerbosch algorithm on the graph, and writes the results in a "results.txt" file (in the same directory).
void test_graph(const std::string&);
